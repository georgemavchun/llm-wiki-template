# Customizations

Record every deliberate deviation from the upstream template here, one line each. When a new template version arrives, this file tells you (and the agent) what to re-apply and what to leave alone.

Template version this copy started from: **0.1.0** (see `CHANGELOG.md`).

| Date | Area | Change | Why |
|---|---|---|---|
| {{START_DATE}} | setup | Copy created for {{OWNER_NAME}} | — |

Areas worth listing: `wiki/CLAUDE.md` language or sensitivity rules, added page families in `wiki.config.json`, extra skills, hook changes, share destinations, CI changes.
